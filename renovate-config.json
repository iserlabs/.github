{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "description": "Org-wide inherited Renovate config for Iser Labs. Applies to every repo in the org when the Renovate app has `inheritConfig` enabled (Mend cloud: org setting; self-hosted: RENOVATE_INHERIT_CONFIG=true). Until then this file is dormant. The primary job is keeping each site's `@iserlabs/web-kit` git-tag pin current (observability standard R6/R7).",
  "labels": [
    "dependencies"
  ],
  "git-submodules": {
    "enabled": false
  },
  "packageRules": [
    {
      "matchPackageNames": [
        "@iserlabs/web-kit"
      ],
      "groupName": "iserlabs web-kit",
      "labels": [
        "dependencies",
        "iserlabs-internal"
      ],
      "addLabels": [
        "web-kit"
      ],
      "semanticCommitType": "chore",
      "commitMessageTopic": "@iserlabs/web-kit",
      "automerge": false,
      "minimumReleaseAge": "0 days"
    },
    {
      "description": "Never auto-adopt a major bump of any dep \u2014 open the PR, let a human review (R7: a bad release must not propagate fleet-wide).",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    },
    {
      "description": "Hold @vitejs/plugin-react on v4 \u2014 v6 requires vite ^8 + React Compiler peers (@rolldown/plugin-babel, babel-plugin-react-compiler), an inappropriate migration for these marketing sites' vitest tooling with no production benefit.",
      "matchPackageNames": [
        "@vitejs/plugin-react"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "enabled": false
    }
  ],
  "vulnerabilityAlerts": {
    "labels": [
      "security"
    ]
  }
}
